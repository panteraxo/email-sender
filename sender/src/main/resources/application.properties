# Puerto del servidor
server.port=8080

# Configuración del servidor de correo (Ejemplo con Gmail)
spring.mail.host=smtp.strato.com
spring.mail.port=587
spring.mail.username=${EMAIL_USER}
spring.mail.password=${EMAIL_PASS}


spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true # Opcional, pero recomendado

# Correo electrónico donde recibirás los mensajes del formulario
app.email.to=pankthera@gmail.com

# Logging (opcional, para ver más detalles)
# logging.level.org.springframework.mail=DEBUG